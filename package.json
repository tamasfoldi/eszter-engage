{
  "name": "eszter-engage",
  "version": "1.0.0",
  "private": true,
  "description": "Eszter meg engage tag?",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-images.mjs",
    "dev": "node scripts/build-images.mjs && npx --yes serve public"
  },
  "devDependencies": {
    "sharp": "^0.35.4"
  }
}
